import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // The React Compiler babel plugin isn't enabled for this project
      // (no --react-compiler at scaffold time), so this rule is flagging a
      // build-time optimization that never actually runs - downgrade it to
      // a warning rather than fighting it across every derived-value +
      // useMemo pattern in a large, data-heavy app like this one.
      "react-hooks/preserve-manual-memoization": "warn",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
